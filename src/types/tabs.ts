export interface TabItem {
    id: string         // e.g., "home", "products", "prompts", "mini-tools"
    label: string      // e.g., "Home", "Products"
    show: string       // "true" or "false" from the sheet
  }